import { Routes } from '@angular/router';

import { CalendarComponent } from './calendar.component';

/** Calendar routes. */
export const calendarRoutes: Routes = [{ path: '', component: CalendarComponent }];
